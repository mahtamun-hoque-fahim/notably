import NotesApp from "@/components/notes/NotesApp";

export const metadata = {
  title: "Your Notes — Notably",
  description: "Your voice notes for today.",
};

export default function NotesPage() {
  return <NotesApp />;
}
